<Window x:Class="SimuladorSO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimuladorSO"
        mc:Ignorable="d"
        Title="Simulador Sistema Operacional" Height="650" Width="900"
        Icon="/Imagem/Icone.ico">

    <Window.Resources>
        <!-- Fonte clean -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <!-- Botões arredondados -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#6C63FF"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>

        <!-- Caixas de texto -->
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0,3,0,3"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Bordas como "cards" -->
        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#EEE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Caixa de saída -->
        <Style TargetType="TextBox" x:Key="OutputBox">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
    </Window.Resources>

    <!-- Layout principal -->
    <Grid Background="#F4F5FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Painel lateral com ScrollViewer -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="10">
            <StackPanel>
                <!-- Card: Gerenciamento de Processos -->
                <Border>
                    <StackPanel>
                        <TextBlock Text="Gerenciamento de Processos" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                        <Button Content="Ver Processos e Threads" Name="btnVerProcessos" Click="btnVerProcessos_Click"/>
                        <Separator Margin="0,10"/>
                        <TextBlock Text="Adicionar Novo Processo" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>

                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="ID do Processo:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Name="txtProcessoIdAdd" Grid.Row="0" Grid.Column="1"/>

                            <TextBlock Text="ID da Thread:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,0"/>
                            <TextBox Name="txtThreadIdAdd" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0"/>

                            <TextBlock Text="Tempo de Execução:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,0"/>
                            <TextBox Name="txtTempoExecucaoAdd" Grid.Row="2" Grid.Column="1" Margin="0,5,0,0"/>

                            <TextBlock Text="Endereço:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,0"/>
                            <TextBox Name="txtEnderecoMemoriaAdd" Grid.Row="3" Grid.Column="1" Margin="0,5,0,0"/>

                            <TextBlock Text="Prioridade:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,0"/>
                            <TextBox Name="txtPrioridadeAdd" Grid.Row="4" Grid.Column="1" Margin="0,5,0,0"/>
                        </Grid>

                        <Button Content="Adicionar Processo" Name="btnAdicionarProcesso" Click="btnAdicionarProcesso_Click" Margin="0,10,0,0"/>
                        <Separator Margin="0,10"/>
                        <TextBlock Text="Remover Processo" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="ID do Processo:" VerticalAlignment="Center" Margin="0,0,5,0" Grid.Column="0"/>
                            <TextBox Name="txtProcessoIdRemover" Grid.Column="1"/>
                        </Grid>
                        <Button Content="Remover Processo" Name="btnRemoverProcesso" Click="btnRemoverProcesso_Click" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card: Escalonamento -->
                <Border>
                    <StackPanel>
                        <TextBlock Text="Escalonamento" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                        <Button Content="FCFS" Name="btnFCFS" Click="btnFCFS_Click"/>
                        <Button Content="SJF" Name="btnSJF" Click="btnSJF_Click"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="RR Quantum: " VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Name="txtQuantumRR" Width="50" Text="3"/>
                            <Button Content="RR" Name="btnRR" Click="btnRR_Click" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Card: Memória e Tempo -->
                <Border>
                    <StackPanel>
                        <TextBlock Text="Memória e Tempo" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                        <Button Content="Alocação de Memória" Name="btnAlocacaoMemoria" Click="btnAlocacaoMemoria_Click"/>
                        <Button Content="Tempo de Execução Total" Name="btnTempoExecucao" Click="btnTempoExecucao_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Painel principal -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Saída do Simulador" FontWeight="Bold" FontSize="16" Margin="0,0,0,5" Grid.Row="0"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <TextBox Name="txtSaida" 
                         Style="{StaticResource OutputBox}"
                         IsReadOnly="True" 
                         TextWrapping="NoWrap" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Auto"
                         MinHeight="400"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>